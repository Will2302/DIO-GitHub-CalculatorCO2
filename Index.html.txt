<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Calculadora EcoTrip</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <div class="container">
        <h1>🛫 Calculadora EcoTrip</h1>
        <p>Simulador de Impacto Ambiental para Viagens</p>
        
        <form id="tripForm">
            <div class="input-group">
                <label>Origem:</label>
                <input type="text" id="origin" list="cities" placeholder="Digite a cidade de origem">
            </div>
            <div class="input-group">
                <label>Destino:</label>
                <input type="text" id="destination" list="cities" placeholder="Digite a cidade de destino">
            </div>
            <div class="input-group">
                <label>Distância (km):</label>
                <input type="number" id="distance" placeholder="Calculado automaticamente" readonly>
                <button type="button" id="manualDistance">Inserir manual</button>
            </div>
            <div class="input-group">
                <label>Modo de Transporte:</label>
                <select id="transport">
                    <option value="">Selecione...</option>
                </select>
            </div>
            <button type="submit">Calcular Emissão</button>
        </form>

        <div id="results" class="results hidden">
            <h2>Resultados</h2>
            <p>Emissão: <span id="emission"></span> kg CO₂</p>
            <div class="comparison">
                <h3>Comparação com outros transportes</h3>
                <div id="bars"></div>
            </div>
            <p>Créditos de carbono: <span id="credits"></span></p>
            <p>Custo estimado: R$ <span id="cost"></span></p>
        </div>
    </div>
    <datalist id="cities">
        <!-- Cidades pré-cadastradas serão populadas via JS -->
    </datalist>
    <script src="script.js"></script>
</body>
</html>
